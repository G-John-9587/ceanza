server:
  port: 8900

# 全局日期格式化
spring:
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  datasource:
    url: jdbc:mysql://106.13.7.243:3306/eyiya?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: xiFENG@1987**
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    show-sql: true
